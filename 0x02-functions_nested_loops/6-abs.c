#include "main.h"

/**
 * _abs - returns the absloute value of an integer
 *@n: The integer to compute.
 *
 * Return: The absloute value of n
 */
int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	return (-n);
}
